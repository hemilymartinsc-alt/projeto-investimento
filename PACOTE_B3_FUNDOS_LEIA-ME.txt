CORREÇÃO CONSOLIDADA — B3 ISIN

Esta correção foi criada após o primeiro push do pacote consolidado revelar
duas condições reais:

1) o comando pytest precisava ser executado como `python -m pytest` no runner;
2) EMISSOR.TXT contém códigos de emissor repetidos (exemplo observado: AGMO).

A correção:
- preserva duplicidades oficiais em vez de descartá-las;
- bloqueia qualquer ponte ambígua;
- cria uma view fail-closed apenas para ISIN -> emissor -> CNPJ unívoco;
- corrige o status de coletas_log para INICIADO;
- desativa o workflow legado automático;
- não altera investimento.ativos;
- no push, faz apenas teste + auditoria sem banco.

NÃO aplicar a Migration 013 manualmente antes de o workflow de auditoria ficar verde.
