name: LEGADO - B3 Inspecao ISIN desativada

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Aviso
        run: |
          echo "Workflow legado desativado."
          echo "Use: B3 - Cadastro oficial ISIN."
